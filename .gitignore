# secrets - never commit
.env
*.local
STATE.md

# python venv + caches
memori/.venv/
**/__pycache__/
*.pyc

# runtime state / logs (regenerated; live data is backed up separately)
memori/data/
mineru/*.log
manage/*.log
manage/usage.db
backups/

# backup artifacts (the backup script writes outside the repo, but just in case)
*.tar.gz
*.bak
*.dump
*.zip
*.log
*.pid

# local/generated SkillsUSA data
skillsusa/*.pdf
skillsusa/output/
skillsusa/knowledge-cards/
skillsusa/knowledge-cards-generated/
skillsusa/knowledge-cards-equipment-stage/
skillsusa_logo.png

# machine-specific scheduled task exports
scripts/scheduled-tasks/*.xml
